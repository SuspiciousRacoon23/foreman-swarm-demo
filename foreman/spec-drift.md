# Spec-drift report

Run `01057ccd-699a-4c6d-b59b-315c8a864117` · **⚠️ drift detected · score 62/100**

> The checksum implementation and synthetic-generator tests are correct, but the six-layer validation pipeline, the entire error-code taxonomy, the tsup dual-output build, CHANGELOG, README, and mandatory bad-checksum mutation tests are all absent or non-conformant. The delivered validateGSTIN also violates the r3 privacy constraint by echoing input sub-fields in error messages.

| Status | Requirement | Note |
|---|---|---|
| 🔴 missing | tsup dual-output (CJS+ESM) is chosen for broadest embedding compatibility without forcing a bundler on consumers (o1+o2, c3) | packages/gstin/package.json points 'main' and 'exports' directly at './src/index.ts'. No tsup.config.ts or build script is delivered. Consumers receive raw TypeScript, not a dual-format dist. |
| 🔴 missing | Six validation layers must execute in strict order with early-return on failure. Length is checked first (=== 15, not regex) to eliminate the ReDoS attack surface before any pattern matching occurs (c1, r4) | validateGSTIN performs a single GSTIN_RE regex test that implicitly encodes length, state code, PAN structure, entity code, Z-marker, and format in one pass. No distinct length === 15 guard precedes the regex, violating the ReDoS mitigation requirement. |
| 🔴 missing | State code is validated via a pre-built Set<string> — no regex alternation — so a 14-char near-miss cannot trigger catastrophic backtracking (r4) | KNOWN_STATE_CODES is defined in packages/audit/src/gst-corpus.ts but is never imported or consulted inside validateGSTIN. The regex [0-9]{2} matches any two-digit state code including invalid ones like '00', '38'–'99'. |
| 🔴 missing | Three remediation classes map to six specific error codes: INVALID_LENGTH, INVALID_PAN_SEGMENT, INVALID_ENTITY_CODE, INVALID_Z_MARKER, INVALID_STATE_CODE, INVALID_CHECKSUM (c2, r3) | validateGSTIN throws only two generic Error instances with no error-code property or enum. None of the six required codes (INVALID_LENGTH, INVALID_PAN_SEGMENT, INVALID_ENTITY_CODE, INVALID_Z_MARKER, INVALID_STATE_CODE, INVALID_CHECKSUM) are defined or thrown anywhere in the delivered files. |
| 🔴 missing | Error messages must never echo, decompose, or name sub-fields of the input string to prevent leaking that the caller possesses a third party's PAN (r3) | validateGSTIN throws `GSTIN format invalid: "${gstin}"` (echoes the full input, exposing the PAN segment) and `GSTIN check digit mismatch: expected "${expected}", got "${gstin[14]}"` (echoes check digit characters). Both violate the r3 PAN-leakage prevention constraint. |
| 🔴 missing | At least 3 known-bad-checksum cases produced by mutating the last character of a valid synthetic GSTIN, confirming the rejection path is exercised independently of generation (r5, o2) | Neither tests/gstin-check-digit.test.ts nor tests/gstin-synthetic.test.ts contains any test that mutates the final character of a synthetic GSTIN and asserts that validateGSTIN throws INVALID_CHECKSUM. The checksum rejection path is untested. |
| 🔴 missing | Callers are advised in the README to pin a semver range and watch releases (r2) | No README file is delivered anywhere in the artifact set. |
| 🟡 partial | KNOWN_STATE_CODES is a named export from the single-file pure-computation library; library version is explicitly tied to this corpus in CHANGELOG (r2) | KNOWN_STATE_CODES is correctly defined as a named export with codes 01–37, but it lives in packages/audit/src/gst-corpus.ts, a separate package from the main @foreman/gstin library. No CHANGELOG is delivered at all. |
| ✅ met | computeCheckDigit must be cross-validated against ≥5 known-good fixtures before any other code depends on it (f1, r1) | Seven test cases cover fixtures 1–6 plus a purity assertion. Fixtures 3 and 4 are explicit canaries for the two known divergence points (carry reduction and double-mod). All fixtures are commented with hand-traced intermediate values. |
| ✅ met | All GSTINs in the test corpus are synthetic — generated programmatically using fictional PANs; no real GSTIN is ever committed to the repo (r5, o2) | All test strings use PANs with 4th char='P' and numeric segment='0000', documented as trivially non-real. generateSyntheticGSTIN is used for integration tests. |
| ✅ met | computeCheckDigit implements GSTN spec verbatim: carry=floor(p/36)+p%36; final index=(36-sum%36)%36 (f1, f3, r1) | Implementation matches the specification exactly. Carry formula and final double-mod are both correct. |
| ✅ met | Single-file pure-computation library with no runtime deps; no network calls (o1+o2, c3, c4) | packages/gstin/src/index.ts has no imports and makes no I/O calls. The library is pure computation. |

_Generated by Foreman's spec-drift detector — every build diffed against its brief._
